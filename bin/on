#!/usr/bin/env bash

if [ -z "$1" ]; then
	echo "Error: File name required"
	exit 1
fi

fn=$(date "+%Y-%m-%d")_$(echo "$@" | tr ' ' '-').md
cd "$HOME/notes/inbox" || exit 1

fp="$HOME/notes/inbox/${fn}"

touch "$fp"
nvim "$fp"
