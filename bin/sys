#!/bin/bash

case "$(printf "s\nh\nk\nr" | rofi -dmenu -i -lines 4)" in
	s) shutdown now ;;
	r) systemctl reboot -i ;;
	k) ps -u "$USER" -o pid,comm,%cpu,%mem | rofi -dmenu -i -p "Kill:" -lines 10 | awk '{print $1}' | xargs -r kill ;;
	h) systemctl hibernate;;
	*) exit 1 ;;
esac
